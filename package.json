{
    "name": "book-it",
    "version": "1.0.0",
    "scripts": {
        "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
        "build": "cd frontend && npm install && npm run build && mv dist ../backend/",
        "start": "node backend/src/server.js"
    }
}
