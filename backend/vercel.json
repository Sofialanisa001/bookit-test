{
    "version": 2,
    "functions": {
        "src/server.js": {
            "includeFiles": "src/public/**"
        }
    },
    "routes": [
        { "src": "/api/(.*)", "dest": "src/server.js" },
        { "src": "/(.*)", "dest": "src/server.js" }
    ]
}
